# Core (existing mini quant stack)
numpy>=1.24
pandas>=2.0

# Alpaca paper trading + WebSockets
websockets>=12.0
msgpack>=1.0.0

# Loads local environment variables from `.env`
python-dotenv>=1.0.0

# Decision Intelligence Terminal backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
